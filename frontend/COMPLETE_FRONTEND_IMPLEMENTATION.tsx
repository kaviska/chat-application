// This file is a compatibility/backup file and has been emptied to avoid
// duplicate component definitions during the TypeScript build. The
// application uses dedicated component files under `components/` and
// page files under `app/`. Keeping this file empty prevents redeclaration
// errors and reduces confusion.

export {};
